name: vProfile Docker Build CI (non-docker)

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - '.github/workflows/vpro-docker-nondocker.yml'
      - 'vprofile-docker/**'
  pull_request:
    branches:
      - main
    paths:
      - '.github/workflows/vpro-docker-nondocker.yml'
      - 'vprofile-docker/**'

permissions:
  contents: read

jobs:
  build:
    runs-on: [self-hosted, Linux, X64, ci]
    defaults:
      run:
        working-directory: vprofile-docker
    steps:
      - name: Code Checkout
        uses: actions/checkout@v5

      - name: Build
        run: |
          # mvn install -DskipTests
          echo "Build step - placeholder for actual build commands"

      # - name: Upload Build Artifacts (WAR)
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: vprofile-app
      #     path: target/*.war

      - name: Notify if Build Fails
        if: failure()
        run: |
          echo "Build job failed! Please check the logs."

  testing:
    runs-on: [self-hosted, Linux, X64, ci]
    needs: build
    defaults:
      run:
        working-directory: vprofile-docker
    steps:
      - name: Code Checkout
        uses: actions/checkout@v5

      - name: Run tests
        run: |
          # mvn test
          echo "Testing step - placeholder for actual test commands"

      - name: Checkstyle
        run: |
          # mvn checkstyle:check
          echo "Checkstyle step - placeholder for actual checkstyle commands"

  # security_scan:
  #   runs-on: [self-hosted, Linux, X64, ci]
  #   needs: build
  #   defaults:
  #     run:
  #       working-directory: vprofile-docker
  #   steps:
  #     - name: Code Checkout
  #       uses: actions/checkout@v5

  #     - name: Trivy Filesystem Scan
  #       uses: aquasecurity/trivy-action@0.28.0
  #       with:
  #         scan-type: fs
  #         scan-ref: .
  #         format: json
  #         exit-code: 0
  #         vuln-type: os, library
  #         output: trivy-results.json

  #     - name: Upload Trivy Scan Results
  #       uses: actions/upload-artifact@v4
  #       with:
  #         name: trivy-scan-results
  #         path: trivy-results.json